schema: 'src/schema.ts'
generates:
    # It should be .d.ts, but Jest doesn't understand that
    src/generated/graphql.ts:
        plugins:
            - 'typescript'
            - 'typescript-operations'
            - 'typescript-resolvers'
